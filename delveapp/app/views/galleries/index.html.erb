<h1>Galleries</h1>

<% @galleries.each do |gallery| %>

<p><strong><%= gallery.name %>,   <%= gallery.gallery_category %></strong>
<em><%= link_to "Explore art at #{gallery.name}", gallery_path(gallery) %></em></p>
<% end %>
